#include <iostream>
#include <iomanip>

using namespace std;
int main(){
    int a, b;
    cin >> a >> b;
    cout << "Binh phuong cua tong 2 so la: " << (a + b)*(a + b);
    cout << "Hieu 2 so la: " << a-b;
    cout << "Tong 2 so la: " << a+b;
    return 0;
}